"""Server-side storage backends (PostgreSQL on pio06).

The Pi/Sheets pipeline is untouched by this package: everything here is
env-gated (ARGIA_PG_MIRROR=1) and becomes a silent no-op elsewhere.
"""
