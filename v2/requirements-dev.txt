pytest>=8.0.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
responses>=0.25.0
freezegun>=1.4.0

# the setup and session-login services are Flask apps; their route
# tests (test_auth_app.py) importorskip when this is absent, so CI
# needs it to actually exercise login/logout rather than skip them
flask>=3.0.0
